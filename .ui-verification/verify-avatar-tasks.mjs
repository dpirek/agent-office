import fs from 'node:fs';
import assert from 'node:assert/strict';
const targets = await (await fetch('http://127.0.0.1:9337/json')).json();
const ws = new WebSocket(targets.find(t => t.type === 'page').webSocketDebuggerUrl);
await new Promise(r => ws.onopen = r);
let id = 0; const pending = new Map();
ws.onmessage = e => { const m = JSON.parse(e.data); if (m.id) { const p = pending.get(m.id); pending.delete(m.id); m.error ? p.reject(m.error) : p.resolve(m.result); } };
const send = (method, params={}) => new Promise((resolve,reject) => { pending.set(++id,{resolve,reject}); ws.send(JSON.stringify({id,method,params})); });
const evaluate = async expression => { const r = await send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true}); if(r.exceptionDetails) throw Error(r.exceptionDetails.text); return r.result.value; };
const pause = () => new Promise(r => setTimeout(r,700));
await send('Page.enable');
await send('Page.addScriptToEvaluateOnNewDocument',{source:`history.replaceState(null,'','/settings'); const originalFetch = window.fetch.bind(window); window.fetch = (url, options) => String(url).startsWith('/api/') ? Promise.resolve(new Response(JSON.stringify({user:{id:'preview',name:'Preview',email:'preview@example.com',role:'member',avatar:'designer'},projects:[{id:'central-office',name:'Central Office'}],prompts:[],skills:[],tasks:[{id:'preview-task',projectId:'central-office',title:'Review the new design',agent:'Design Agent',status:'pending',priority:'medium',progress:0,createdAt:Date.now()}],operations:[],workers:[],messages:[],entries:[],logs:[],tree:[],toolPermissions:{},mcpConfig:{},providerSettings:{}}),{headers:{'Content-Type':'application/json'}})) : originalFetch(url,options);`});
await send('Emulation.setDeviceMetricsOverride',{width:1440,height:960,deviceScaleFactor:1,mobile:false});
await send('Page.navigate',{url:'http://127.0.0.1:8129/index.html'}); await pause();

await evaluate(`document.documentElement.dataset.theme='teams'; document.querySelector('[data-section="account"]').click()`); await pause();
await evaluate(`document.querySelector('.account-avatar-picker summary').click()`);
assert.equal(await evaluate(`document.querySelectorAll('input[name="avatar"]').length`),9);
assert.equal(await evaluate(`document.querySelector('.account-avatar img').getAttribute('src')`),'/assets/avatars/designer.png');
await evaluate(`document.querySelector('input[value="designer"]').click(); document.querySelector('.avatar-choice-form').requestSubmit()`); await pause();
assert.equal(await evaluate(`document.querySelector('.avatar-save-status').textContent`),'Avatar saved.');
let shot=await send('Page.captureScreenshot',{format:'png'}); fs.writeFileSync('screenshots/account-avatar-picker.png',Buffer.from(shot.data,'base64'));
await evaluate(`document.querySelector('[data-section="dashboard"]').click()`); await pause();
assert.equal(await evaluate(`document.querySelector('.dashboard-tasks-panel').hidden`),false);
assert.equal(await evaluate(`document.querySelector('.dashboard-task strong').textContent`),'Review the new design');
shot=await send('Page.captureScreenshot',{format:'png'}); fs.writeFileSync('screenshots/dashboard-tasks-widget.png',Buffer.from(shot.data,'base64'));
await send('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:1,mobile:true});
await evaluate(`document.querySelector('[data-section="account"]').click()`); await pause();
await evaluate(`document.querySelector('.account-avatar-picker summary').click()`);
assert.equal(await evaluate(`document.documentElement.scrollWidth <= innerWidth`),true);
console.log('PASS: avatar choices, profile preview, save interaction, dashboard task rendering, and mobile account width (fixture API).');
await send('Browser.close'); ws.close();
