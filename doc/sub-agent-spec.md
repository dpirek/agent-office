# Agent Worker

Agent workers now use the authenticated, worker-initiated WebSocket protocol. The former HTTP task submission and callback protocol is no longer supported.

See [Worker WebSocket API](./worker-websocket-api.md) for registration, task, progress-update, completion, and deliverable message formats.
